# LLM — Google Gen AI SDK (Vertex AI: set vertexai + GCP project/location)
google-genai>=1.0.0

# UI
streamlit>=1.40.0

# Database
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.0

# Observability
langfuse>=2.0.0,<3.0.0

# App utilities
pydantic-settings>=2.6.0
pandas>=2.2.0

# Phase 2 — NL SQL + charts (DuckDB over CSVs, Seaborn)
duckdb>=1.1.0
matplotlib>=3.9.0
seaborn>=0.13.0

# Phase 3 — NL→SQL (numpy for embedding cosine retrieval)
numpy>=2.0.0